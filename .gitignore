# Local project config — copy from project-config.yml.example per app repo
project-config.yml

# Local MCP credentials
.cursor/mcp.json

# Vendored MCP server dependencies (dist/ is committed)
.cursor/mcp-servers/**/node_modules
